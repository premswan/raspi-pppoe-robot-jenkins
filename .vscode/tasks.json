{
  "version": "2.0.0",
  "tasks": [
    {
      "label": "Run PPPoE one-test Robot suite locally",
      "type": "shell",
      "command": "python -m robot --outputdir results --variable RPI_HOST:192.168.1.2 --variable MIRROR_IFACE:eth0 robot/tests",
      "group": "test",
      "problemMatcher": []
    }
  ]
}
